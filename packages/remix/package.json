{
	"name": "@circ/remix",
	"version": "0.1.0-alpha.9",
	"description": "Circuitry for Remix",
	"main": "dist/index.js",
	"private": false,
	"types": "dist/index.d.ts",
	"scripts": {
		"build": "tsc && renamer --find .js --replace .mjs dist/**/*.js",
		"prepublish": "npm run build",
		"preversion": "tsc --noEmit"
	},
	"devDependencies": {
		"@circ/circuit": "^0.1.0-alpha.7",
		"renamer": "^4.0.0",
		"typescript": "^5.0.4",
		"zod": "^3.0.0"
	},
	"peerDependencies": {
		"@circ/circuit": "^0.1.0-alpha.0",
		"zod": "^3.0.0"
	}
}
