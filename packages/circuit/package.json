{
	"name": "@circ/circuit",
	"version": "0.1.0-alpha.10",
	"description": "Circuitry for frameworks",
	"main": "dist/index.js",
	"private": false,
	"types": "dist/index.d.ts",
	"scripts": {
		"build": "tsc --build --clean && renamer --find .js --replace .mjs dist/**/*.js",
		"prepublish": "npm run build",
		"preversion": "tsc --noEmit"
	},
	"devDependencies": {
		"@swc/cli": "^0.1.62",
		"@swc/core": "^1.3.49",
		"renamer": "^4.0.0",
		"typescript": "^5.0.4"
	}
}
